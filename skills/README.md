# Skills

Each active skill lives in its own directory with a `SKILL.md` file. The file begins with YAML front matter containing at least `name`, `slug`, `description`, and `status: active`, followed by instructions, guardrails, procedure, output schema, and verification guidance. The loader makes the Markdown body the source of truth for prompts.

