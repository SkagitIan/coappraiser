# Version comparison

Each revised upload creates a new immutable `ReviewVersion`. Findings use a stable rule/location signature. The review page reports fixed, still-present, and new signatures against the prior version. Decisions remain auditable and are not deleted when a user dismisses or accepts risk.
