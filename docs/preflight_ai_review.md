# AI review

AI remains a focused interpretation layer behind deterministic extraction. It should receive only relevant normalized evidence, return validated structured output, preserve prompt/model versions, and clearly label interpretation versus extracted fact. It must never determine value, fabricate facts, or claim official validation.
